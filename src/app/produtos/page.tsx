import { createProduct } from "@/app/actions/mvp";import { WorkspaceFrame } from "@/components/workspace-frame";import { getRequiredWorkspaceContext } from "@/lib/auth/web-session";import { prisma } from "@/lib/db/prisma";
export default async function Page(){const c=await getRequiredWorkspaceContext();const products=await prisma.product.findMany({where:{organizationId:c.organization.id},include:{balances:{where:{storeId:c.store.id}}},orderBy:{name:"asc"}});return <WorkspaceFrame><header className="tireflow-page-head"><div><p className="tireflow-eyebrow">CATÁLOGO</p><h1 className="tireflow-title">Produtos e Pneus</h1></div></header><form action={createProduct} className="tireflow-card mvp-form"><select name="type" required><option value="TIRE">Pneu</option><option value="PRODUCT">Produto</option><option value="SERVICE">Serviço</option></select>{["category","name","brand","model","size","rim","internalCode","barcode","purchasePrice","salePrice","minimumStock"].map(k=><input key={k} name={k} required={["category","name","internalCode"].includes(k)} type={k.includes("Price")||k==="rim"||k==="minimumStock"?"number":"text"} step={k.includes("Price")?".01":undefined} placeholder={k}/>) }<button className="tireflow-btn primary">Novo produto</button></form><div className="tireflow-card tireflow-table-wrap"><table className="tireflow-table"><thead><tr><th>Produto</th><th>Tipo</th><th>Marca/medida</th><th>Código</th><th>Saldo</th><th>Venda</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><strong>{p.name}</strong><small>{p.category}</small></td><td>{p.type}</td><td>{p.brand} {p.size}</td><td>{p.internalCode}</td><td>{p.balances[0]?.quantity??0}</td><td>R$ {Number(p.salePrice).toFixed(2)}</td></tr>)}</tbody></table></div></WorkspaceFrame>}
