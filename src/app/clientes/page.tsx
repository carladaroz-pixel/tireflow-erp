import { createCustomer } from "@/app/actions/mvp";import { WorkspaceFrame } from "@/components/workspace-frame";import { getRequiredWorkspaceContext } from "@/lib/auth/web-session";import { prisma } from "@/lib/db/prisma";
export default async function Page(){const c=await getRequiredWorkspaceContext();const rows=await prisma.customer.findMany({where:{organizationId:c.organization.id},orderBy:{name:"asc"}});return <WorkspaceFrame><h1 className="tireflow-title">Clientes</h1><form action={createCustomer} className="tireflow-card mvp-form">{["name","taxId","phone","whatsapp","email","licensePlate","vehicleModel","notes"].map(k=><input name={k} key={k} required={["name","phone","whatsapp","licensePlate","vehicleModel"].includes(k)} placeholder={k}/>) }<button className="tireflow-btn primary">Cadastrar cliente</button></form><div className="tireflow-card tireflow-table-wrap"><table className="tireflow-table"><thead><tr><th>Nome</th><th>Telefone</th><th>WhatsApp</th><th>Veículo</th><th>Placa</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.phone}</td><td>{x.whatsapp}</td><td>{x.vehicleModel}</td><td>{x.licensePlate}</td></tr>)}</tbody></table></div></WorkspaceFrame>}
